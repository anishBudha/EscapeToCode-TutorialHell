```
-> creating a react app: npx create-react-app react_app_name
                        npx create-react-app . // for creating the app in the same folder
- For the project, minial react app by lama dev:-> git clone --single-branch -b "react-mini" https://github.com/safak/youtube2022.git .
// its the same create-react-app files but without unnecessary items
-> npm install or yarn // to start

- To navigate between different pages we will be using "react router dom"
-> yarn add react-router-dom or npm add react-router-dom
- Import the required react-router-dom components: should be like:
import {
    .....
}
in the docs
- Copy the router provider and create the router (inside we will add the paths)

- handling the navbar and footer using the react-router-dom "Outlet" (so not to code in every pages)

- installed sass: -> npm add sass
- installed mui: -> npm install @mui/material @emotion/react @emotion/styled
- installed mui library: -> npm add @mui/icons-material

```

> https://github.com/safak/youtube2022/tree/ecommerce
> 53:27
